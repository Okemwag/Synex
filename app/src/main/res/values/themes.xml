<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Synex" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@color/synex_paper</item>
        <item name="colorPrimary">@color/synex_ink</item>
    </style>

    <style name="Theme.Synex" parent="Base.Theme.Synex" />

    <style name="Theme.Synex.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/synex_teal</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/synex_logo</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/synex_teal</item>
        <item name="windowSplashScreenAnimationDuration">700</item>
        <item name="postSplashScreenTheme">@style/Theme.Synex</item>
    </style>
</resources>
